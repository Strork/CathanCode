#include <bits/stdc++.h>
using namespace std;
unordered_map<string, string> m;
string s, fa;

int main() {
    while (cin >> s) {
        if (s[0] == '$') break;
        if (s[0] == '#') {
            
        }
    }
    return 0;
}